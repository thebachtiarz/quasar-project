import router from 'src/router'
var awaitSleep = {
  sleep (ms) {
    return new Promise(resolve => setTimeout(resolve, ms))
  },
  async redirectTo (url, time = 3000) {
    await this.sleep(time)
    router.push({ name: url })
  }
}
export default awaitSleep
