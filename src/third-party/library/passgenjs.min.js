import PassGen from "generate-password";
const PassGenJs = {
    newGenPass: () => PassGen.generate({ length: 10, numbers: true, symbols: true, excludeSimilarCharacters: true })
};
export default PassGenJs;