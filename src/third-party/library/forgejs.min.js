import forge from 'node-forge'
var ForgeJs = {
  encryptPassword (password) {
    const encs = forge.md.sha384.create()
    const encn = forge.md.sha512.create()
    encs.update(password)
    encn.update(encs.digest().toHex())
    return encn.digest().toHex()
  }
}
export default ForgeJs
